root: /root/hiwell
required_files:
  - ops.json
  - banned-ips.json

stages:
  production:
    user: root
    host: minecraft.iskrenkov.com
    docker:
      compose:
        filename: docker-compose-production.yml
