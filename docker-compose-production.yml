version: '3.9'

services:
  hiwell:
    image: ghcr.io/eiskrenkov/hiwell:latest
    env_file:
      - .env
    ports:
      - 25565:25565
    volumes:
      - universe:/hiwell/universe

      - ./ops.json:/hiwell/ops.json
      - ./banned-ips.json:/hiwell/banned-ips.json

volumes:
  universe:
